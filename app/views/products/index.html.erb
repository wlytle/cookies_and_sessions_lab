<h1>Cart</h1>
<ul>
    <% cart.each do |i| %>
        <li><%= i %></li>
    <% end %>
</ul>

<%= form_tag(url: products_path, method: "post") do %>
    <%= label_tag :product, "product" %>
    <%= text_field_tag :product %>
    <%= submit_tag "Add Item" %>
<% end %>